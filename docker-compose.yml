version: '3'
services:
    app:
        build: .
        ports:
            - 8888:8888
        environment: 
            CONDA_ENV: ma-simulation
        volumes:
            - ./code/:/usr/app/
            - ./data/ :/usr/app/data/
        stdin_open: true
        tty: true